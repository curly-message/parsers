# Changelog

## Unreleased

Initial version line for `@curly-message/parser`.

* Sources moved verbatim from `@sveltekit-i18n/parser-curly` — same behavior,
  same tests, new home. The published `@sveltekit-i18n/*` line keeps its own
  changelog; this one starts here.
