name: JavaScript parser tests

on:
  push:
    paths:
      - js/**
      - .github/workflows/tests-js.yml
      - .github/workflows/tests.yml
  workflow_dispatch:

jobs:
  tests:
    uses: ./.github/workflows/tests.yml
    with:
      package: js
